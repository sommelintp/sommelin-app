# 設計: 認可の統一（IDOR封じ ＋ 業務ロール承認制）

2026-07-24 Opus 5 設計（コード実査で全事実を検証）。正本。外部セキュリティ監査 #1 IDOR / #2 ロール昇格 への回答。

## 0. 監査指摘と、実査で分かった「本当の姿」

監査は「全ての個人データAPIでSupabase JWTを検証し、JWTから対象UIDを解決せよ」と述べた。方向は正しい。ただし**そのままでは適用できない**ことが実査で判明した。

- `somm_uid` は **localStorageのランダムUUID**（scouter.html:1014 他）。認証トークンとは無関係。DBの `anon_user_id` の実体。
- 個人データAPIは `req.query.userId` をそのまま信用（account.js:330 他）。**UUIDを知る＝データを読める**＝秘匿性に依存した「認可もどき」。

**しかし決定的な発見:**

| 部品 | 実体 | 状態 |
|---|---|---|
| 匿名ユーザーのJWT | `getAnonAccessToken()`（sommelier-chat.html:694・prototype.html:5038）が Supabase の匿名サインアップ（`/auth/v1/signup` 空ボディ）で**本物のJWTを取得済み**。`somm_anon_session` に保持 | **既に稼働**（チャットのクォータ判定でのみ使用） |
| トークン検証 | `verifyAuthUser(req)`（identify.js） | **既に稼働** |
| uid↔アカウント対応表 | `user_devices(auth_user_id, somm_uid)` ＋ `uidsOf()` | **既に稼働** |

→ **匿名ユーザーも検証可能な身分証を既に持っている。個人データAPIがそれを見ていないだけ。**
本設計は新機構を作らず、**既存部品を結線する**（[[sommelin-keep-in-step-principle]]）。

## 1. 原則：「userIdは名乗り、JWTは証明」

クライアントが送る `userId` は**ヒント**に降格する。認可はサーバがJWTから導く。
- トークンが有効 → `auth_user_id` → `user_devices` → **その人が触ってよい somm_uid の集合**
- 要求された `userId` がその集合に無い → **403**
- トークン無し → 個人データは **401**（公開データは従来どおり）

## 2. エンドポイントの3分類（一律適用は事故になる）

35箇所が `userId` を受けているが、危険度は同じではない。分類して扱いを変える。

**A. 個人データ（厳格・JWT必須）** ← IDORの本体
`/api/cellar`(GET/POST)・`/api/cellars`系・`/api/taste/list`・`/api/taste/*`(削除含む)・`/api/xp`系・`/api/filters/mine`・`/api/wine-like`・`/api/wine-like/shared` 等（account.js の userId 系ほぼ全部、identify.js:6403/6430/6462）
→ `authorizedUids()` を通す。無トークンは401。

**B. 匿名スキャン系（緩やか・可用性優先）**
`/api/identify-*`・`/api/chat`・`/api/back-label` 等。ここでの `userId` は**好み学習とクォータの鍵**であって他人のデータを読み出す口ではない。
→ トークンがあれば紐付け検証、無ければ**従来どおり動かす**（no-lieの可用性維持）。ただし他人のuidを詐称して好みプロファイルを汚す余地は残るため、**トークンがあるのに集合外のuidを名乗った場合のみ403**。

**C. 公開データ（変更なし）**
`/api/wine-search`・`/api/producer-wines`・`/api/place-ig`(GET)・`/api/places/*` 等。

## 3. #1 実装設計（IDOR封じ）

### 3.1 サーバ: 共通ヘルパー1本
```js
// 呼び出し元が触ってよい somm_uid 集合を返す。null=認可失敗（401/403は呼び出し側で判定）
async function authorizedUids(req, requestedUid) {
  const authId = await verifyAuthUser(req);        // 既存部品
  if (!authId) return null;                         // Aでは401
  const { data } = await supabase.from('user_devices')
    .select('somm_uid').eq('auth_user_id', authId);
  const owned = (data || []).map(r => r.somm_uid);
  if (!requestedUid) return owned;
  return owned.includes(requestedUid) ? [requestedUid] : null;  // 集合外=403
}
```
既存の `uidsOf(sommUid)`（クライアント入力起点）を、**この関数に置き換える**のが修正の実体。

### 3.2 束縛（bind）: somm_uid を身分証に結び付ける
現状 `/api/link-device`(account.js:57) は **ログインさえしていれば任意の `sommUid` を自分に紐付けられる**（監査指摘のとおり・実在）。これが残ると③の認可が意味を失う。

**先着1回のみ有効（first-claim-wins）に変更:**
- その `somm_uid` が **未束縛** → 要求者に束縛（正常系。既存ユーザーは次回アクセス時に自動で自分のものになる）
- **既に別の auth_user_id に束縛済み** → **403**（他人のデータの乗っ取り不可）
- 同一 auth_user_id → 冪等にOK

UUIDv4 の推測は非現実的なため、「攻撃者が正規ユーザーより先に束縛する」経路は事実上塞がる。

### 3.3 フロント: 匿名JWTを全ページへ
- `getAnonAccessToken()` が **3ファイルに重複**（sommelier-chat / prototype / 他）→ **`assets/somm-auth.js` に単一ソース化**（[[sommelin-keep-in-step-principle]]）。
- 個人データAPIを叩く全fetchに `Authorization: Bearer` を付与。
- 初回に `/api/link-device`（＝bind）を1回呼ぶ。

### 3.4 移行の安全策（重要）
既存ユーザーは `user_devices` に行が無い可能性が高い。**いきなり401にすると全員が自分のデータを見失う。**
→ **2段階リリース**:
1. **Phase A（監視モード）**: 認可判定は実施するが、失敗時は**拒否せずログのみ**（`authz_would_block` を app_events へ）。数日運用し、正規ユーザーが弾かれないことをテレメトリで確認。
2. **Phase B（強制）**: 403/401を実際に返す。
これは「壊さずに直す」ための必須手順。**Phase Aを飛ばさない。**

## 4. #2 実装設計（業務ロール承認制）

### 4.1 現状（実査）
- `/api/account/add-role`(account.js:99): ログインのみで `store`/`retailer`/`importer` を**自己付与**（承認確認なし）
- `verifyRole()`(identify.js:3283): `profiles.role/roles` を見るだけで **`verification_status` を見ていない**
- 一方 **`profiles.verification_status` は既に存在**し `'approved'`/`'pending'` を business.js が管理（business.js:274/287）＝**確認基盤は既にある。参照していないだけ。**

### 4.2 設計：「希望」と「権限」を分離
- `add-role` は **希望ロールの申請**に降格 → `profiles.requested_roles` へ記録（実権限 `roles` は書き換えない）
- 実権限への昇格は **`verification_status='approved'` になった時のみ**（business.js の確認フロー完了時にサーバ側で `roles` を更新）
- `verifyRole(req, allowed)` に **`verification_status==='approved'` チェックを追加**（`admin` はSQL手動付与のため対象外）

### 4.3 移行の安全策（重要）
**デプロイ前に必須の確認**: 現在 `store`/`retailer`/`importer` を持つユーザーが何人いて、その `verification_status` が何かを数える。
- 既に `approved` の人 → そのまま通る（影響なし）
- **`approved` でない既存業務ユーザー** → いきなり締め出すと実利用者が困る。**個別に承認へ移すか、猶予期間を設ける**（オーナー判断が要る）。
→ この数え上げをやるまで #2 は本番反映しない。

## 5. 段階リリース計画

| 段階 | 内容 | モデル |
|---|---|---|
| **S1** | 既存業務ユーザーの棚卸し（4.3のカウント）＋ `authorizedUids`/bind 実装＋**Phase A(監視のみ)** | Sonnet（設計確定済み） |
| **S2** | テレメトリ確認 → **Phase B(強制)** へ | Sonnet＋オーナー確認 |
| **S3** | #2 ロール承認制（4.3の判断後） | Sonnet |
| **S4** | 残り: OAuth鍵必須化・X-Forwarded-For・RLS・lockfile・Googleログイン | Sonnet |

## 6. やらないこと（誤検知・過剰対応の回避）
- **業務店URL確認のSSRF対策は既に実装済み**（business.js:74-102）。監査の指摘は誤り。手を入れない。
- **名刺OCRのfail-open**は該当コード(`verifyCardFront`)が存在せず再現不能。追わない。
- localStorage→HttpOnly Cookie 移行は、GitHub Pages(静的配信)＋Cloud Run(別オリジン)構成では**同一サイトCookieが使えない**ため現実的でない。**CSP強化＋XSS防止**で守る方が費用対効果が高い（S4で扱う）。

## 関連
[[sommelin-security-audit-2026-07-24]] [[sommelin-business-verification-design]] [[sommelin-data-architecture-principle]] [[sommelin-keep-in-step-principle]]
