# 設計確定書: アカウント紐付け(12)・経験値(9)・ホームフィード(4)
2026-07-07 Fable設計。実装はSonnetが本書に従って行う。**着手順: 12 → 9 → 4**（12が土台。9と4は12のID解決に乗る）。

## 前提（確認済みの事実）
- 公開系データは全て匿名UUID `somm_uid` 基準: `filters.owner_uid`・`filter_items.owner_uid`・`user_follows.anon_uid`・`taste_items.anon_user_id`・`wine_likes.anon_user_id`
- 認証は本物（Supabase Auth・`profiles.id`=auth.users.id・`invited_by`記録済み）だが、上記の匿名データと未接続
- 端末ローカルのみ: `somm_cellar_v1`（セラー）・`sommelin_visits`（来店）・`sommelin_nickname`
- scouterはログイン不要で使える（この体験は壊さない＝無料フェーズ戦略）

---

## 12. アカウント紐付け — 設計決定

**方式: IDマップ方式（既存データの書き換えはしない）**

匿名UUIDはそのまま使い続け、「このauthユーザーはこれらのsomm_uidを持つ」という対応表を1枚作る。既存テーブルのスキーマ・既存行は一切変更しない（低リスク・可逆）。

### 新テーブル
```sql
create table user_devices (
  auth_user_id uuid not null,          -- auth.users.id
  somm_uid     text not null,          -- 端末のlocalStorage UUID
  linked_at    timestamptz default now(),
  primary key (auth_user_id, somm_uid)
);
create index idx_user_devices_uid on user_devices (somm_uid);
-- RLS有効・ポリシー無し（backend service roleのみ。somm_uidは秘匿情報）
```

### 紐付けの流れ
1. backend `POST /api/link-device` { token, sommUid } — tokenをsupabase.auth.getUser()で検証→user_devicesへupsert（冪等）
2. フロント共通スニペット（sommLogと同じ場所）に「`sommelin_session`と`somm_uid`が両方あれば/api/link-deviceを叩く」を追加。**全ページ・毎ロードで自己修復**（新端末でログインした瞬間に自動リンク）
3. マージは不要: 読み取り側が「本人の全somm_uid」をIN句で引くだけ。破壊的処理ゼロ

### backend共通ヘルパ
```js
async function uidsOf(userIdOrToken) → [somm_uid, ...]
// somm_uidを渡されたら: user_devicesで同じauth_user_idを持つ全somm_uidに展開（未リンクなら自分だけ）
```
既存の読み取り系（/api/taste/summary・loadTasteAgg・フィルター一覧等）を順次 `eq(uid)` → `in(uids)` に差し替え。**書き込みは従来通りその端末のsomm_uidで行う**（書き換え不要の根拠）。

### 端末ローカルデータのサーバー移行（セラー・来店）
```sql
create table user_cellar (
  id uuid primary key default gen_random_uuid(),
  somm_uid text not null,
  client_row_id text,                  -- 端末側の行ID（upsert用）
  name text, vintage text, region text, producer text,
  cat text, bottles int, note text,
  updated_at timestamptz default now(),
  unique (somm_uid, client_row_id)
);
create table user_visits (
  id uuid primary key default gen_random_uuid(),
  somm_uid text not null, rid text not null, name text, ts timestamptz default now()
);
-- 両方RLS有効・ポリシー無し（backend経由のみ）
```
- API: GET/POST `/api/cellar`・`/api/visits`（GETはuidsOf()で全端末分を統合して返す）
- cellar.html / prototype.html: localStorageを**オフラインキャッシュ**に降格。ロード時にGET→localStorage更新→描画、変更時にPOST（失敗してもローカルには残す）
- ニックネームは既に`profiles.display_name`に入る仕組みがあるので追加作業なし（匿名ユーザーはローカルのまま＝登録時に引き継がれる）

### ログインの強制はしない
scouter等は匿名のまま使える。マイページ・セラーに「アカウントを作るとデータがどの端末でも見られます」の控えめなバナーを出すだけ（誘導はするが強制しない）。

---

## 9. 経験値システム — 設計決定

### XPの記録（サーバー側で付与＝改ざん防止。将来の特典の土台）
```sql
create table xp_events (
  id uuid primary key default gen_random_uuid(),
  somm_uid text not null,
  event_type text not null,   -- 下記ルールのキー
  points int not null,
  meta jsonb,
  created_at timestamptz default now()
);
create index idx_xp_uid on xp_events (somm_uid);
-- RLS有効・ポリシー無し
```
backend `awardXp(sommUid, type, meta)` ヘルパを作り、**既存エンドポイントの成功時に仕込む**（フロントからXP直付与のAPIは作らない）:

| event_type | 点 | 仕込み場所 | 上限 |
|---|---|---|---|
| scan | 2 | /api/identify-shelf, /api/identify-list 成功時 | 20回/日 |
| wine_like | 5 | /api/wine-like | 20回/日 |
| filter_add | 10 | /api/filters/items | （既存の50/日に乗る） |
| review_bonus | 20 | filter_addのcommentが**100文字以上**の時に加算 | — |
| short_comment | 5 | 同commentが1〜99文字の時に加算 | — |
| ig_share | 10 | /api/wine-like/shared | 10回/日 |
| referral | 30 | **紹介の親子両方に+30**。invited_by書き込み検知（下記） | — |

- referralの発火: /api/link-deviceの中で「このauthユーザーのprofiles.invited_byが埋まっていて、かつxp_eventsに未付与」なら、本人のsomm_uidと親のsomm_uid（uidsOf経由）へ各+30を一度だけ付与（meta に相手のauth_idを入れて重複判定）。**これが14番の「特典」の実体＝XPで払う**（クーポン等の別基盤は作らない）
- 点数・上限はbackend先頭の`XP_RULES`定数1箇所にまとめる（調整しやすく）
- 文字数判定は`[...str].length`（コードポイント。絵文字混じりでも大きくズレない・シンプル優先）

### レベルと称号（固定テーブル10段階）
```
L1 たまごソムリン 0 / L2 ひよこソムリン 30 / L3 見習いソムリン 80 /
L4 ソムリン 160 / L5 中堅ソムリン 280 / L6 匠ソムリン 450 /
L7 賢者ソムリン 700 / L8 マイスターソムリン 1000 / L9 グランソムリン 1400 / L10 レジェンドソムリン 2000
```
- `GET /api/xp?userId=` → { total, level, title, nextAt, topCountry }（uidsOf()で全端末合算）
- topCountry: taste_items+wine_likesのkuni集計の最頻値。**割合40%以上のときだけ**「国推し」認定（乱れた飲み方の人に無理に国を貼らない）

### ソムリン君の成長表示（v1の割り切り）
- マイページ（prototype.html）のプロフィール部にレベル・称号・XPバーを表示。アバターは既存`assets/sommelin/`の画像をレベル帯で切替（1-3=welcome / 4-6=recommend / 7-9=swirling / 10=delighted 等、既存アセット流用）
- **国別の衣装・持ち物はv1では国旗絵文字＋称号で表現**（例:「🇫🇷 フランス通」バッジ）。国別コスチューム画像は存在しないアセットなので、後日アセット制作タスクとして分離（#13に残す）。嘘のない範囲で今出せる形を優先

---

## 4. ホームフィード — 設計決定

### 新API `GET /api/feed?userId=&limit=30`（backend集約・10分キャッシュ不可＝個人化のためキャッシュは軽めに）
今**実在するデータだけ**でカードを組む。生産者返信（Meta審査待ち）・位置情報連動はv2。

カードソースと基本スコア:
| source | 中身 | weight |
|---|---|---|
| follow | フォロー中（user_follows＋reviewer_follows）の人の新着filter_item / 高評価銘柄 | 5 |
| referral | 紹介の親・子・孫（profiles.invited_by×user_devicesでsomm_uid解決）の公開filter_item | 4 |
| similar | 好みが近い人（wine_socialのコサイン類似基盤を流用）の公開filter_item | 3 |
| popular | /api/top-wines上位（既存キャッシュ流用） | 1 |
| nearby | /api/top-places上位のお店カード | 1 |

- 並び順: `score = weight × exp(-hours/72)`（3日で減衰）で単一リストに混ぜる。同一ワインの重複はweight最大のカードだけ残す
- カード形: { type, wine{name,producer,vintage,catalogId,wineId}, person{nickname or filter名・social_id}, rating, comment, reason("フォロー中"/"あなたを招待した人"/"好みが近い人"/"今週の人気"), ts }
- **プライバシー**: 人の表示は`social_profiles`のニックネーム/フィルター名のみ。somm_uid・メール等は絶対に出さない（wine_socialの既存原則を踏襲）

### フロント（prototype.html）
- 新タブ「🏠 ホーム」を追加し**初期表示をホームに変更**（履歴タブは残す。ナビは ホーム/カメラ/ランキング/マイページ/履歴 の並びに）
- ヘッダーにソムリン君＋レベル（/api/xpから。9番の成果をここに載せる）
- カードのアクション: 「🔍 探す」（既存buy()流用）「📌 詳しく」（wine_search遷移）。フィードからの❤️はv2（scouter経由の体験を先に固める）
- 空状態（フォロー0・紹介0の初期ユーザー）: popular+nearbyのみでも成立する構成にする（コールドスタートでも空にならない）
- 各カード表示・タップをsommLog（feed_view/feed_tap + source種別）で記録＝**どのソースが刺さるかのデータを初日から取る**

---

## 実装順（Sonnet向けチェックリスト）
1. SQL: user_devices / user_cellar / user_visits / xp_events（4テーブル一括・SQL Editor実行）
2. backend: uidsOf()ヘルパ → /api/link-device → awardXp()＋XP_RULES → 既存エンドポイントへの仕込み → /api/xp → /api/cellar・/api/visits → /api/feed
3. フロント共通: link-device自己修復スニペット（sommLog隣接に全ページ配置）
4. cellar.html・prototype.html履歴: サーバー同期化（localStorage=キャッシュ化）
5. prototype.html: ホームタブ新設＋初期表示変更＋XPヘッダー＋マイページのレベル表示
6. 動作確認: 匿名→ログイン→別端末ログインでデータが繋がること／XP付与／フィードのコールドスタート表示
