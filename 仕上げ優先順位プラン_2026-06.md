# ソムリンアプリ 仕上げ優先順位プラン（2026-06-27）

対象: 本番フロント `~/dev/sommelin-app` を点検した結果と、「他の部分を完璧に仕上げる」ための順番。
前提: スカウター（ビジョンAI版）は継続ブラッシュアップ扱い。当面は一般ユーザーの「飲んだワイン備忘録」でデータを貯める。

---

## 1. 現状サマリ（本番点検）

| 画面 | データ連携 | 状態 | 主な残作業 |
|---|---|---|---|
| `scouter.html` | Supabase直結(read/rpc) | P0稼働 | ビジョンAI版は別途（継続BU） |
| `wine_search.html` | 一部Supabase | 概ね動く | 安定化・`name_romanized` 充填 |
| `importer_admin.html` | read有/**書込=仮(localStorage)** | **書込9箇所が未実装** | events保存・顧客/店舗編集・saved_events・broadcasts(LINE)・catalog(Storage)。全て「auth+RLS前提」 |
| `restaurant_admin.html` | 一部Supabase/書込仮 | 同上パターン | 保存をSupabase化 |
| `login.html`・`onboarding.html` | localStorage | **本物の認証なし** | Supabase Auth＋役割分け＋RLSの土台 |
| `line_liff_register.html` | 連携なし | 登録UIのみ | 送信先・保存の実装 |
| `cellar.html` | localStorage | 端末保存で動作 | アカウント同期（認証後） |
| `sommelier-chat.html` | なし | モック | 実AI化（backend `/api`） |
| `winelist`/`wine_ar`/`index`/`privacy`/`terms` | 静的 | 表示のみ | `wine_ar`＝旧ARデモ |

### importer_admin の未実装9箇所（`TODO(backend)`）
1. `importer_store_links` UPDATE（店舗リンク）
2. `events` INSERT（イベント作成）※`events` と `tasting_events` のスキーマ乖離を要確定
3. `saved_events` INSERT/DELETE（保存）
4. `person_links` UPDATE（顧客リンク）
5–7. 店舗情報の編集/削除（`persistStoreEdits`）
8. `broadcasts` INSERT＋**LINE Messaging API push**（配信・既読）
9. `importer_catalog` UPSERT＋**Storage（写真）**（カタログDB化）

→ すべて **認証＋RLS** が前提。だから順番が決まる（下記）。

---

## 2. 仕上げの順番（依存関係で決定）

### Phase 1 ─ 認証の土台（最優先・すべての鍵）
`login.html`/`onboarding.html` を **Supabase Auth で本物に**。役割（インポーター/酒販・卸/飲食・ホテル/個人）で出し分け＋RLS。
**なぜ最初か**: 管理画面の保存9箇所は全部「auth+RLS前提」。ここが無いと他を安全に完成できない。

### Phase 2 ─ インポーター管理を本番化（中核SaaS）
`importer_admin` の書込をSupabaseへ。順序: ①events作成→保存（スキーマ確定込み）→ ②顧客/店舗編集・saved_events → ③カタログ（Storage）→ ④broadcasts＋LINE配信（最重・最後）。
`restaurant_admin` も同じパターンで横展開。

### Phase 3 ─ ユーザー機能の地固め
`cellar` をアカウント同期、`wine_search` 安定化、**「飲んだ備忘録」を cellar/検索に統合**（スカウターの種データ）、`sommelier-chat` を実AI化（backend）。

### Phase 4 ─ データ整備
フル `wines`（約62,566件）投入完了＋`name_romanized` 充填、`reviewer_hub` の承認運用。

---

## 3. 注記
- 多くが backend（Cloud Run）＋Supabase スキーマ作業を伴う。**Phase 2 着手前に `events` vs `tasting_events` のスキーマ乖離を確定**（本番実テーブルは `events`）。
- デプロイはユーザーの「フロント-デプロイ」。SQLは Supabase SQL Editor で実行（規約通り）。
- キャラ/ロゴは公式アセットをそのまま使用（改変しない）。

## 4. 推奨の次の一手
**Phase 1（認証）から**。ここが全管理機能の土台。OKなら、まず現状の `login.html`/`onboarding.html` と必要なSupabase Auth設定を確認し、実装計画に落とします。
