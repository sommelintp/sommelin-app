# ソムリンアプリ プロジェクト運用手順

最終更新: 2026-06-19

このプロジェクトを複数のチャットでうまく回すための手順です。新しいチャットを始めるとき、まずこれに沿って進めれば迷いません。
（AIが自動で読む簡潔版は同じフォルダの `CLAUDE.md`）

## 0. 大原則（これだけは）

1. **編集する場所は `~/dev/sommelin-app` だけ。** iCloud の古いコピーは触らない。
2. **公開は「フロント-デプロイ」をダブルクリック。** Claude はコードを直すところまで、公開はあなたが実行。
3. **チャットは用件ごとに分け、重くなったら新しく作る。** 引き継ぎはコード(git)とメモが持つので、会話を続けなくても大丈夫。

## 1. フォルダの地図

- フロント本番: `~/dev/sommelin-app`（git管理・GitHub Pages へ公開）← **ここを編集**
- 旧コピー（使わない）: `~/Documents/Claude/Projects/ソムリンアプリ`（iCloud・実データ未連携の古い版）
- backend: `~/dev/sommelin-trade-platform`
- 公開URL: https://sommelintp.github.io/sommelin-app/

※ Cowork 起動時に旧コピー側が開いていたら、Claude に「`~/dev/sommelin-app` を使って」と言えば接続して切り替えます。

## 2. 1チャットの基本フロー

1. **用件を1つに絞って依頼する。** 例:「importer_admin の○○を△△にして」。画面名（§7の早見表）を言うと速い。
2. Claude が該当ファイルを編集。
3. デスクトップの「フロント-デプロイ」をダブルクリックして公開。
4. 「完了」と伝える → Claude が公開URLで表示確認＋必要なら携帯での見方を案内。
5. 直したい点が出たら 2 に戻る。テーマが変わる／会話が長くなったら新チャットへ。

## 3. デプロイと確認

- **デプロイ:** デスクトップの「フロント-デプロイ」をダブルクリック（git add→commit→push）。反映に1〜2分。
- **確認:** ページをリロード（`?cb=数字` などでキャッシュ回避）。カレンダーは初期表示が当月。
- うまく見えない時は §5 へ。

## 4. 依頼のコツ（チャットを効率化）

- 「どの画面の・どこを・どうしたい」をセットで伝える。
- 画面を見てほしい時は「URL出して」、確認してほしい時は「確認して」。
- 仕様の決定や前提（料金設計、命名ルールなど）は「覚えておいて」と言うとメモに残り、次のチャットでも効きます。

## 5. つまずいた時

- **ESET が「フィッシング」警告でサイトをブロック:** 自分のサイトの誤検知です。ESET で許可するか、スマホで開けば見られます（Claude は警告を突破しません）。
- **git commit が bus error:** iCloud 起因。`rm -f .git/index && git reset` のあと、もう一度デプロイ。（`~/dev` に移したので基本は起きません）
- **古い表示が出る:** 1〜2分待ってリロード／キャッシュ回避。
- **SQL:** Supabase の SQL Editor で実行（ターミナルに貼らない）。

## 6. なぜチャットを分けるのか（「消費大」問題）

会話が長くなりすぎると、Claude のツール操作が実行直前で壊れ、作業が途中で止まったように見えます（"malformed" エラー）。同じチャットで粘るほど悪化します。対策はシンプルで、**用件ごとにチャットを分ける／重くなったら新規チャットに移る**こと。コードは git、決定事項はメモが覚えているので、新しいチャットでも続きから動けます。

## 7. 画面（ファイル）早見表

- `prototype.html` — 顧客（お客さん）向けアプリ
- `importer_admin.html` — インポーター管理（カレンダー / 発信 / 顧客 / カタログ）
- `restaurant_admin.html` — 酒販店・飲食店向け管理
- `login.html` / `onboarding.html` — ログイン・登録・役割分け（インポーター / 酒販店・卸 / 飲食店・ホテル / 個人）
- `cellar.html` `wine_search.html` `wine_ar.html` `sommelier-chat.html` — 各機能ページ

## 8. データ作業の流れ（参考）

- ワインマスターの監査 → 確認用 xlsx 作成 → あなたが承認 → Supabase / Google シートへ反映。
- 詳細は過去の「データ監査」チャットを参照。
