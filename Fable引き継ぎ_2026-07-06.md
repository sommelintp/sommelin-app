# Fable引き継ぎメモ（2026-07-06作成・7/7更新・Sonnetセッションからの積み残し）

7/8デモ本番の推し活（❤️評価→フィルター公開→Instagramシェア）は実装・デプロイ済み。0・1・2(暫定処置のみ)・3・8・14(記録の最小実装のみ)番はSonnetで実装・デプロイ完了。

**⚠️ 今回読むのは【今回対象】が付いた 4・9・12番だけにしてください。それ以外（2・5〜7・10・11・13・14の残り）は絶対に読まないでください（詳細本文を読むのはトークンの無駄・今回のスコープ外）。見出しだけ見て「後でやる」と認識すれば十分です。**

---

## 0.【済】scouterの照合先をwine_catalogに完全移行（コスパ計算含む）

- **原因確定済み**: `identify.js`の`matchMaster()`が`supabase.rpc('match_wines', ...)`経由で今も古い`wines`テーブル（15,563件）だけを検索しており、7/5に投入した`wine_catalog`（67,034件・頭脳側の完成データ）には未接続。wine_catalogにしかないワインは実際にはDBに載っているのに、scouterでスキャンすると「未登録」扱いになる（オーナー報告と一致）。
- **これは`wine_catalog移行計画.md`のPhase 2（未着手）そのもの**。単純な差し替えでは済まない点: scouterの「コスパ」表示は`wines.osu_gbm`列前提の計算で、wine_catalog側には未実装（移行計画では「今日のコスパ」= `evaluation_model_by_year.json`を使った新計算式を別途実装する予定になっている）。
- **オーナー指定の対応方針（2026-07-06）**: 応急処置（wines/wine_catalogの二重検索等）ではなく、コスパ計算も含めてちゃんと全部繋ぎ変える本格移行を選択。
- **やること**: ①RPC `match_catalog(q,lim)`作成（name_ja/name_en/producer_ja/producer_enのtrgm照合、多言語対応） ②`matchMaster()`をcatalog版に差し替え（レスポンスは互換形: name=name_ja, producer=producer_ja, iro=color_ja, kuni=country_ja, chiiki=region1_ja, price=price_jpy + catalogId追加） ③「今日のコスパ」計算の実装。詳細は`wine_catalog移行計画.md`のPhase 2記述を参照（そのまま実行する形になる）。

---

## 1. （0番に統合）scouterで過去に評価済みのワインが反映されない不具合

- オーナー確認済み：内容は実質0番と同じ。0番（wine_catalog未接続の解消）に含めて対応する。個別の作業項目としては扱わない。

---

## 2.【暫定処置済・本設計は未着手】推し活の星評価ロジックが単純すぎる

- **現状**: `scouter.html`の`oshiPaintScore()`が `Math.round(oshiRating/2)` で10点満点0.1刻みの評価を機械的に5段階★へ変換しているだけ（8.0点→★4、のような線形変換）。`filters/items`への保存時も同じ変換（`oshiSaveToFilter()`）。
- **あるべき姿（オーナー指摘）**: 「本当のおすすめ度」は単純な点数変換ではなく、①**そのユーザー個人の採点の傾向**（甘め/辛めの補正・正規化）と②**価格に見合っているか**（コスパ。既存の`osu_gbm`/cospaバッジの考え方と同種）を両方加味して決まるべき。8点だから星4、という短絡的な対応をやめたい。
- **関連する既存の仕組み**: `wines.osu_gbm`/`osu_lin`（SP−価格から期待されるSPとの差＝コスパの既存ロジック）、cospaバッジの表示ロジック（scouter.htmlの`cospa()`関数）。
- **設計判断が要る点**: 「個人の採点傾向を正規化する」統計モデルの設計（ユーザーごとの評価分布・平均・分散をどう蓄積して補正に使うか。データ量が少ないユーザーへのコールドスタート対応も必要）。価格考慮の重みづけと採点傾向補正をどう合成して最終的な★数（またはそれに代わる指標）にするか。

---

## 3.【済】ソムリンAI質問ボタンの配置

- **現状**: 「ソムリンAIに質問」ボタンが常にマイページに固定されている。
- **あるべき姿**: 配置を変更できるようにしてほしい（オーナー曰く「今はマイページのところにあってうざい」）。
- **関連する既存の仕組み**: `sommelier-chat.html`への導線。マイページ側の実装箇所は要特定（`prototype.html`のマイページタブ内と推測、未確認）。
- **設計判断が要る点**: 「動かせるように」の解釈（ユーザーがドラッグで位置を選べるカスタムUIにするのか、単に運営側で配置場所を変える／フローティングボタン化するのか）を最初に決める必要あり。

---

## 4.【今回対象】アプリ起動時の最初のページをSNS的な発見フィードに

- **現状**: 個人ユーザーのホーム（`prototype.html`、login.htmlの`roleHome.individual`）は情報量が少ない静的な画面。
- **あるべき姿**: みんなが推薦したワイン・生産者からの返信/反応・近くの美味しいワイン等が流れる、SNSのフィードのような画面にしたい。
- **フィード優先順位ロジック（2026-07-06追加指定）**: 表示優先度は ①フォロー中の人 ②関係性の強い人（相互やり取りが多い等） ③紹介の親（自分を招待してくれた人） ④紹介の子（自分が招待した人） ⑤紹介の孫（子がさらに招待した人）。加えて好みの傾向が近い人から「この人があげてるワインはあなたも興味あるでしょ？」的な協調フィルタリング的レコメンドも組み込む。#18（紹介ネットワーク）と密接に関連。
- **関連する既存の仕組み**: 推し活（#5・フィルター公開）、`wine_social.html`（フォロー中の人の好みワイン表示）、`/api/top-wines`・`/api/wine-places`（近くで飲める/買える）、生産者からの返信の仕組みは現状未実装（15章仕様書のPhase2「生産者へ届く通知」が前提）。
- **設計判断が要る点**: フィード全体の情報設計（何を・どの順で・どれくらいの頻度で出すか）。「関係性の強さ」の定量化方法（相互やり取り回数？滞在時間？要設計）。生産者からの反応を表示するには、そもそも生産者側の反応を検知/記録する仕組み（Instagram mentions API連携 or 手動運営投稿）が先に要る＝依存関係の整理が必要。

---

## 5. お店（レストラン・ワインショップ）の推し活

- **現状**: `onboarding.html`の「お気に入りのお店」ミッション（`shop1`/`shop3`）は入力させるだけの見せかけ。他ユーザーに公開されず、Instagram共有もない。
- **あるべき姿**: ワイン版（❤️評価→フィルター公開→Instagram共有）と同じ型をお店にも適用したい。推し活の対象はワインだけでなくお店も、とオーナー指定。
- **関連する既存の仕組み**: `restaurant_settings`・`places`（店舗マスター）、`profiles.instagram`（店舗アカウントのIGハンドル、2026-07-04追加済み）、`/api/top-places`（店の評価集計ロジック）。
- **設計判断が要る点**: 「お店」の一意な特定方法（公式店=`restaurant_settings`と、街で発見した非公式店=`places`の2系統がある。両方に対応するか公式店限定にするか）。評価の保存先テーブル設計（wine_likes相当の新テーブルが要る）。

---

## 6. ワインテイスター（レビュアー）の推し活

- **現状**: `reviewer_hub.html`に「フォロー」機能（`reviewer_follows`テーブル、単純トグルのみ）はあるが、評価・コメント・Instagram共有はない。
- **あるべき姿**: ワイン版と同じ型（❤️評価→コメント→Instagram共有＝テイスター自身の@ハンドルへメンション）を追加。「フォロー」とは別軸の「この人のレビュー参考になった/推せる」という推し活。
- **関連する既存の仕組み**: `reviewer_follows`、`profiles.instagram`（レビュアーのIGハンドル）、`rater_profiles.base_weight`（信頼度加重ロジック、流用できる可能性）。
- **設計判断が要る点**: 「テイスターを推す」という行為の単位（個別の投稿・レビューに対してか、テイスターという人物全体に対してか）。フォロー機能との併存UI設計。

---

## 7. 全ページのデザイン刷新（frontend-designスキル導入済み）

- **現状**: Anthropic公式の`frontend-design`スキルを`~/dev/sommelin-app/.claude/skills/frontend-design/`に導入済み（`git clone https://github.com/anthropics/skills.git`から取得）。約25ページ全てが対象、オーナーは「時間がかかってもいい、本気で全部やり直す」と明言。
- **あるべき姿**: 既存ブランド（バーガンディ`#9B1B4B`・ゴールド`#C9973A`・ソムリン君キャラクター）は活かしつつ、"AIっぽい没個性なUI"を避けた具体的な意匠に刷新。
- **関連する既存の仕組み**: 各ページのCSS変数・カラー定義（現状ページごとに散在、統一トークン化されていない）。
- **設計判断が要る点（最優先でFableに委ねたい部分）**: デザイントークン体系そのもの（色・タイポグラフィペア・レイアウトコンセプト・signature要素）をまず1つ固める。その後の個別ページ適用はSonnetでも巻き取れる想定。

---

## 8.【済】scouterの表示改善（軽微・3件まとめ）

- **2-1. ワイン名/生産者名/ヴィンテージの表示**: 見づらい/分かりにくいというFB（詳細未確認、実機で再現ヒアリング要）。
- **2-2. SP（ソムリン評価点）が前面に出過ぎ**: ランキング画面で既に行った「SP等の呼称を表示からは排除し内部計算のみ残す」ブランド抑制方針を、scouterの詳細カードにも適用したい。**（2026-07-06精緻化）** SPは「ソムリンのフィルター（推薦リスト）を持っている人」＝ある程度深く関わっているユーザーにだけ表示する。フィルターを持たない人には無意味・ありがた迷惑になるため、代わりに「関係性の強い人（フォロー中・紹介関係等）の評価を集約したおすすめ度（MAX5星）」を表示する、という出し分けロジックが必要（#18の紹介ネットワーク・#4のフィード優先順位ロジックと関連）。
- **2-3. 絵柄のみ（文字なし）ラベルのボトルが認識できない**: Vision AIプロンプト（`VISION_SHELF_SYSTEM`等）にラベルデザイン・絵柄からの推定手がかりを追加する方針を検討。

---

## 9.【今回対象】経験値システム＋ソムリン君の成長・国別カスタマイズ

- **現状**: `onboarding.html`に既に`stages`配列＋`updateScore()`（0-100点のスコア→ステージごとの絵文字＋称号＋レベルアップ演出）という仕組みがある。ただしオンボーディング完了時の一時的なスコアであり、その後の恒常的な活動（レビュー投稿・写真撮影）に紐づくXPではない。ステージ表示は絵文字ベースで、`assets/sommelin/`配下の画像アセット（表情違い複数：`sommelin-delighted.png`等）とは別系統。
- **あるべき姿（オーナー指定）**: 写真を撮ってオススメワインを紹介すると経験値が貯まる。レビューをちゃんと書く（100文字以上）ほど、写真だけ撮るより経験値が多く増える。経験値が上がるとソムリン君が成長する。さらに、よく飲む国の傾向（例: フランスワインばかり）に応じて、ソムリン君の持ち物・服装が変わる（フランス国旗を持たせる、フランス風の服にする等）。
- **関連する既存の仕組み**: `onboarding.html`の`stages`/`updateScore()`（拡張のベースになりうる）、`wine_likes`/`taste_items`（活動履歴・産地データの蓄積先）、`assets/sommelin/`の複数キャラクター画像（衣装バリエーション表現の参考）。
- **設計判断が要る点**: XPの永続化設計（新規テーブル：累計XP・レベル・活動ログが必要）。レビューの「質」判定ロジック（単純な文字数カウントだけで足りるか、内容充実度も見るか）。国別カスタマイズの実装方式（国ごとに画像を用意する静的差し替えか、Canvas/SVGでパーツ合成するか）。onboarding.htmlの既存stages仕組みとの統合可否（作り直すか拡張するか）。

---

## 10. デザインテンプレート付きワインリスト作成機能（印刷対応）

- **現状**: `restaurant_admin.html`に「ワインリスト」パネルが既にある（`restaurant_wines`テーブル・写真一括読込・公開/非公開設定まで実装済み・機能面は動く）。ただし見た目は装飾のない実務的なリスト表示のみで、デザインテンプレートや印刷用レイアウトは無い。`winelist.html`は旧デモの退役スタブで`prototype.html?r=<店ID>`へリダイレクトするのみ（実体はprototype.html側）。
- **あるべき姿（オーナー指定）**: ワインを選ぶだけで簡単にリストを作成。ワインの説明コメントはソムリンAIがいろんな人のコメント・Web資料から生成（※著作権ルール=事実はOK・他媒体の文章そのまま転載NG、既存方針と一致）。作成したリストは履歴が残り、都度編集・調整可能。新しいワインを「今注目！」等で目立たせる加工。複数デザインテンプレートから選べる。ポップ風の装飾要素を自由配置できる。特別ワイン枠のような特殊デザイン領域。印刷可能（PDF/紙メニュー相当）。
- **関連する既存の仕組み**: `restaurant_admin.html`の既存ワインリストパネル（データ面の土台）、`external_ratings`（数値スコア＋出典名＋URLのみ・批評文本文は持たない設計＝AIコメント生成の際の参照元として使える）、著作権ルール（[[sommelin-copyright-rule]]）。
- **設計判断が要る点**: レイアウトの自由配置をどう実装するか（ドラッグ&ドロップのビルダーUIが要るか、テンプレート選択+一部カスタムで十分か）。印刷用CSS（`@media print`）かPDF生成（サーバーサイド生成が要るか）かの方式選択。AIコメント生成のプロンプト設計と「複数ソースから合成」の実装（著作権遵守を担保する仕組み込みで）。対象は飲食店・酒販店（レストラン/ワインショップ）中心と想定されるが、個人ユーザーにも開放するかは要確認。

---

## 11. ワイン評価の多面的細分化（ヴィンテージ・生産者・畑・味→総合点）

- **現状**: 評価はすべて単一の総合数値のみ。`tasting_notes.rating`（★1-5）、`wine_likes.rating`（10点満点0.1刻み、2026-07-06新設）、マスターの`SP`（頭脳側の総合スコア）。いずれも項目別の内訳は持たない。
- **あるべき姿（オーナー案）**: ヴィンテージ・生産者・畑（テロワール）・味など、評価の観点を細分化してユーザーに分かりやすく提示する。ただし最終的には従来通り総合得点にまとめる（項目別評価はあくまで内訳・過程であり、総合点というゴールは変えない）。
- **関連する既存の仕組み**: `wine_likes`（2026-07-06新設・推し活の評価保存先）、`tasting_notes`（★1-5+osu）、マスターSPの算出方法（頭脳側管理・詳細はソムリンDBセッション側）。
- **設計判断が要る点**: 細分化する項目の確定（ヴィンテージ・生産者・畑・味、の4つで十分か、増やすか）。各項目の評価UI（それぞれスライダー/星を出すのか、簡易な3-5段階か）。項目別スコアから総合点への統合方法（単純平均か重み付けか）。既存の単一評価（wine_likes.rating等）のスキーマ変更が必要かどうか（列追加か別テーブルか）。マスターSPとの関係整理（ユーザー評価の細分化とマスターSPは別軸として維持するのか、混ぜるのか＝no-lie原則・SP/コミュニティ評価分離の既存方針[[sommelin-wines-catalog-map]]と整合させる必要）。

---

## 12.【今回対象】データのアカウント紐付け（端末をまたいだ利用）

- **現状**: 履歴（`sommelin_visits`）・セラー（`somm_cellar_v1`）・ニックネーム（`sommelin_nickname`）・好み（`taste_items`、匿名`somm_uid`経由）等、個人ユーザーの主要データがほぼ全て**端末のlocalStorageのみ**に保存されている。ログイン自体はSupabase Authで本物だが（`sbSignUp`/`sbSignIn`実装済み、確認済み2026-07-06）、ログインとこれらのデータは紐づいていない。友人が別端末で開いたら別人として見える（マイページの名前バグの根本原因もこれに近い）。
- **あるべき姿（オーナー指定）**: 今後スマホアプリ化も見据えているので、ウェブでもアプリでも同じデータが見られるようにしたい。食べログのように、どの端末からでも自分のデータにアクセスできる状態を目指す。
- **関連する既存の仕組み**: `sommelin_session`（Supabase Authのセッション。`login.html`）、`profiles`テーブル（`id`=auth.users.id）、匿名`somm_uid`（scouter/taste/推し活で使用中の端末ローカルUUID）。
- **設計判断が要る点**: 匿名UUID方式からログイン必須のアカウント紐付け方式への移行方針（全面的にログイン必須にするか、匿名のまま使い続けられる余地を残すか）。既存の匿名データ（`somm_uid`で溜まった履歴）をログイン後のアカウントにどう引き継ぐか（マージ処理が必要）。cellar/history/taste等、対象ごとに個別テーブル化するかまとめるか。

## 13. 食べログ型のSEO公開ページ→アプリ誘導

- **現状**: `producer.html`は「生産者名から自動生成される生産者ページ」があるが、動的なSEOメタタグ（`<title>`・OGP等）が無く、検索エンジンに個別最適化されていない（確認済み: `<title>ソムリン — 生産者ページ</title>`固定）。個別ワインの専用URL・詳細ページは現状存在しない（`wine_search.html`は検索UIのみで、個々のワインの独立ページには繋がっていない）。
- **あるべき姿（オーナー指定）**: 食べログのように、ウェブで「ワイン」を検索すると、そのワインの情報ページがウェブ上で見られる。そこからアプリへ誘導される導線にしたい。デジタル/ウェブとアプリの役割分担を作る。
- **関連する既存の仕組み**: `producer.html`（自動生成ページの前例）、`wine_catalog`（67,034銘柄・多言語データ、SEOコンテンツの素材になりうる）、GitHub Pagesでの静的ホスティング（クローラーが読める静的HTML生成と相性が良い）。
- **設計判断が要る点**: ウェブ側で「どこまで見せてどこからアプリに誘導するか」の線引き（食べログは店舗基本情報・口コミの一部は無料公開、詳細機能はアプリ/会員限定、という設計）。個別ワインページの生成方式（wine_catalogの67,034件全部を静的生成するか、アクセスされた時に動的生成するか）。SEO対策（構造化データ、サイトマップ等）。#12（アカウント紐付け）と合わせて、ウェブ→アプリの導線でログイン状態をどう引き継ぐか。

---

## 14.【記録は済・特典とネットワーク可視化は未着手】友人紹介の特典システム＋紹介ネットワークの可視化

- **✅ 最小記録は実装・デプロイ済み（2026-07-07）**: `profiles.invited_by`/`invited_at`列を追加。`login.html`が`?ref=<招待者のprofiles.id>`をキャプチャ（`localStorage.somm_ref`、既存値は上書きしない）、サインアップ成功時に`patchProfile()`で書き込み。`onboarding.html`の招待ミッションは実際の個人リンク（`login.html?ref=<自分のUUID>`）を発行するよう修正済み。7/8以降の紹介チェーンのデータは失われない状態。
- 残っているのは**特典の実装とネットワーク可視化**のみ（下記）。
- **現状**: `onboarding.html`に「友達を1人招待」ミッション（`invite`、+10pt）があるが、実際の招待コード発行・追跡・特典付与は無い（`completeMission('invite')`のデモボタンのみ）。
- **あるべき姿（オーナー指定）**: 友人を招待すると、招待した人・された人の**両方に**特典が付くようにしたい。誰が誰を紹介してどう繋がったか（親→子→孫の紹介チェーン）をネットワークとして可視化できると面白い。機能化（特典・可視化）は後でもいいが、**データ（誰がどの招待経由で登録したか）だけは今のうちから取り始めたい**。
- **関連する既存の仕組み**: `onboarding.html`の`invite`ミッション（見せかけの土台）、`profiles`テーブル（招待者IDを持たせる列を追加できる）、`login.html`の`sbSignUp(email,pw,{role})`（メタデータに項目を足せる）。
- **設計判断が要る点**: 招待コード/リンクの発行方式（ユーザー固有のURLパラメータ等）。特典の内容（ポイント？機能解放？）。紹介チェーンのデータモデル（`profiles.invited_by`のような自己参照で足りるか、専用の関係テーブルが要るか）。**最優先で決めるべきは「今すぐ何を記録し始めるか」**＝将来の機能化を待たずに、登録時の招待元だけは今からでも記録できるようにしておく最小実装をどう設計するか。

---

## 依存関係の補足
- #4（ホームフィード）は #5・#6（お店・テイスター推し活）の完成後の方が中身が充実する。
- #7（デザイン刷新）は#1〜#6のUI変更と競合しうるので、着手順序をFableと相談して決めるのが良さそう。
