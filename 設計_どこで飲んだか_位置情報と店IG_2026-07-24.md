# 設計: 「どこで飲んだか」— 位置情報で店候補 ＋ 店Instagramのクラウド蓄積

2026-07-24 Fable設計（コード実査で全事実を検証済み）。正本。オーナー発案（推しカードの「どこで飲みましたか？」ブロックの高度化）。

## 0. オーナーの言葉（要件の源）
- どこで飲んだかは**写真の位置情報から探して、Google Mapから候補となりそうな店を出して**
- その店のInstagramは**ユーザーのデータから保管して、店が特定できたらすぐにアカウントに紐付け**できるように

## 1. まず正直な技術的事実（設計の土台）

**「写真の位置情報(EXIF GPS)」は、棚スキャンでは構造的に取れない。**
- スカウターの既定写真＝ライブカメラ映像を`<canvas>`に描いたもの（`scanPhotoDataUrl()`）。**canvas化でExif/GPSは完全に消える**。
- 「📁写真から」のライブラリ画像も、スキャンに使う時点でcanvas化される。ただし**選択直後のFileオブジェクトには元ExifがOSの共有設定次第で残っている**。Exifを読むならcanvas化前のFile（`handleFiles`/`oshiOnPhotoSelected`）で読むしかない＝実装位置が確定している。
- 一方 **端末GPS（`navigator.geolocation`）＝「今いる場所」**。その場でスキャン→その場で推し活、という主要動線では「飲んでいる場所」と実質同じ。既存の「このお店を記録」(`openPlacePicker`)が既にこの方式で稼働中。

**→ 位置ソースの優先順位（no-lie・実際に取れるものだけを使う）:**
1. ライブラリ写真のGPS Exif（あれば。canvas化前のFileから読む）
2. 端末GPS＝実質の主役
3. どちらも不可/拒否 → 店名手入力（従来通り）

**⚠ ライブラリ写真の罠（Fableレビューで追加）**: 昔の写真を自宅でスキャンした場合、端末GPS=自宅であり「飲んだ場所」ではない。→ **候補は常にユーザーがタップして選ぶ提案であり、自動確定は絶対にしない**（間違った店が静かに付くことを構造的に防ぐ。無関係な候補が並ぶのは「役に立たない」だけで「嘘」ではない）。

## 2. 現状の棚卸し（コード実査済み・既にあるもの＝作らない）

| 機能 | 実体 | 状態 |
|---|---|---|
| 近隣店候補 | `GET /api/places/nearby?lat&lng`（Google Places searchNearby優先→OSM Overpassフォールバック・100m格子6hキャッシュ） | 稼働 |
| 会場サジェスト | `/api/places/suggest`（Autocompleteセッション課金$0最適化）・`/places/detail` | 稼働 |
| 会場マスター | `places`（place_id=`google:`/`osm:`/`manual:`の安定ID） | 稼働 |
| クラウド蓄積 | `venue_list_items`・`price_sightings`（`/api/report-place`） | 稼働 |
| 店の引き継ぎ | `GET /api/venue-claim?googlePlaceId`（店の住所確定→匿名リスト継承） | 稼働 |
| **店↔Place IDの紐付き** | **`restaurant_settings.google_place_id`**（業務店が住所確定した瞬間に保存。[restaurant_admin.html:5312]。**backendはまだ誰も読んでいない**） | **列は稼働・未活用** |
| 端末GPS導線 | `openPlacePicker()`（GPS→nearby→候補/手入力→report-place） | 稼働 |
| 登録店の店IG | `/api/stores?q`（`restaurant_settings`×`profiles.instagram`。**テキスト検索のみ・位置未使用**） | 稼働 |
| 蓄積IGの型 | `producer_ig_candidates`（AI提案→ユーザー確認→pending/active・`validIgHandle()`） | 稼働（#6の手本） |

**問題=2つの川の分断**: 推しカードの「どこで飲みましたか」(`oshiStore*`)は登録店テキスト検索のみ。GPS→Google Places候補は`openPlacePicker`に閉じている。

## 3. 設計の核

### 3.1 川を1本に（#5）: 推しカードの店ブロックに位置候補を流す
- `oshiStoreBlockHtml()`に **「📍 近くのお店から選ぶ」ボタン**を追加。**タップで初めてGPS許可を求める**（カード作成画面を開いただけで許可ダイアログを出さない＝店タグに興味がない人を煩わせない）。Permissions APIで既に`granted`なら自動で候補を出す（聞かれずに済む人には最初から出る）。
- 候補は`/api/places/nearby`（Google Maps由来・距離順）。**タップ選択のみで確定・自動確定なし**（§1の罠）。店名手入力とIG手入力は従来通り残す（保険）。
- 確定した店は`places`のplace_id（`google:`優先）で握る＝業務店確認(venue-claim)と同じ安定ID（足並み）。
- **場所選択のロジックは`openPlacePicker`と共通化**（1つのplace-chooserを両方から使う。二重実装を作らない）。

### 3.2 店IGのクラウド蓄積（#6）: `producer_ig_candidates`の店版
新テーブル **`place_ig_candidates`**（DDL=§5・DBさん依頼）。信頼度3段:
- **解決API `GET /api/place-ig?placeId=`**:
  1. `google:`IDなら **`restaurant_settings.google_place_id`一致の登録業務店** → `profiles.instagram`（最上位。**この列の最初の読者になる**＝紐付きは実査で確認済み）
  2. 無ければ `place_ig_candidates` の active
  3. 無ければ pending →「@xxx で合っていますか？」確認UIへ（生産者IGと同じ作法）
- **投稿API `POST /api/place-ig`**（placeId・handle・action=suggest/confirm/reject）。`validIgHandle()`検証・匿名uidのみ記録。confirm閾値(2)でactive昇格・reject多数でrejected（サーバー側運用）。
- **「店が特定できたらすぐ紐付け」の実現**: place_idがGoogle Place IDである限り、店が後から業務登録して住所確定した瞬間（=`google_place_id`保存の瞬間）、解決APIの1段目が自動的にその店の公式IGへ切り替わる。**蓄積候補→公式への昇格が、コード変更なしに起きる構造**。venue-claimへの候補IG表示はP3で追加。

### 3.3 ついでに拾う価値（Fableレビューで追加）: 推し活の場所をワイン地図の燃料に
店を選んで作った推しカード＝「このワインをこの店で飲んだ」という**一級の目撃情報**。既存の「どこで飲める？買える？」地図はvenue_list_items×placesを燃料にしている。`wine_likes.place_id`列を1本足せば（§5）、推し活のたびに地図が濃くなる。個人情報は増えない（place_idのみ）。

### 3.4 no-lie / プライバシー / コスト
- 未確認IGは確認を挟んでからカードに乗せる（誤@メンションで無関係な店に通知を飛ばさない）
- 蓄積は place_id・handle・匿名uid・件数のみ
- Google Placesは既存キャッシュ＋セッション課金最適化の枠内。新規の外部APIなし（[[sommelin-free-phase-strategy]]）

## 4. 段階分け
- **P1（#5）**: 店ブロックの位置候補化＋place-chooser共通化。**純フロント・Sonnet可**
- **P2（#6）**: DDL（§5）→`/api/place-ig`解決・投稿→店選択時のIG自動補完＋確認チップ＋`wine_likes.place_id`記録
- **P3**: venue-claimに蓄積IG候補の表示追加／ライブラリ写真のExif GPS解析（canvas化前のFileから・自前の軽量APP1パーサ）
- 各段階ともデプロイ前にオーナー確認。DDLは「ソムリンDB」セッションへ（[[sommelin-session-division-of-labor]]）

## 5. DBさんへの依頼（DDL・P2で必要）
```sql
-- 店Instagramのクラウド蓄積（producer_ig_candidatesの店版）
-- 2026-07-24 DBさんレビュー反映（確定版）: id=uuid・status に 'not_found'・evidence_url追加・
-- 匿名uidは wine_likes と同名 anon_user_id。confirm/reject両方は store版のreject flowのため意図的に残す。
create table place_ig_candidates (
  id            uuid primary key default gen_random_uuid(),
  place_id      text not null,                -- google:/osm:/manual:（placesと同じ体系・FKなし）
  handle        text not null check (handle ~ '^[a-z0-9._]{1,30}$'),
  status        text not null default 'pending' check (status in ('pending','active','rejected','not_found')),
  source        text not null default 'user', -- 'user' | 将来 'ai_search'
  evidence_url  text,                          -- ai_search由来のIG発見元（user投稿はnull）
  anon_user_id  text,                          -- 匿名uid（wine_likesと同名・個人情報なし）
  confirm_count int not null default 0,
  reject_count  int not null default 0,
  created_at    timestamptz not null default now(),
  updated_at    timestamptz not null default now(),
  unique (place_id, handle)
);
create index on place_ig_candidates (place_id, status);

-- 推し活の場所をワイン地図の燃料に（§3.3）
alter table wine_likes add column place_id text null;
create index on wine_likes (place_id) where place_id is not null;
```

## 6. 実装順（番号付き・P1から）
1. **【実装済み・2026-07-24 Sonnet】** フロント: place-chooser共通化（`fetchNearbyPlaces`/`geoAlreadyGranted`を`openPlacePicker`から抽出）＋`oshiStoreBlockHtml`へ「📍近くのお店から選ぶ」（タップ起動・granted時は自動＝`oshiNearbyMaybeAuto`）＝P1。デプロイ・実機確認は未（フロント-デプロイ待ち）
2. （DDL後）サーバー: `/api/place-ig` GET/POST＋昇格ルール＝P2
3. フロント: 店選択→place-ig解決→IG自動補完・pending確認チップ。`/api/wine-like`にplace_id同送＝P2
4. サーバー: venue-claimに蓄積IG候補表示＝P3
5. フロント: ライブラリ写真Exif解析（canvas化前のFile）＝P3
6. 各段階デプロイ前にオーナー確認

## 関連
[[sommelin-ig-oshi-flow]]・[[sommelin-business-verification-design]]（venue-claim・google_place_id）・[[sommelin-oshi-card-design]]・[[sommelin-keep-in-step-principle]]・[[sommelin-data-architecture-principle]]
